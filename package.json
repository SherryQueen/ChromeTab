{
  "name": "i-tab",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build && cp ./manifest.json ./dist/manifest.json && mkdir -p ./dist/assets && cp -r ./src/assets/* ./dist/assets && cp -r ./content-scripts ./dist/content-scripts",
    "serve": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.1",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.7",
    "typescript": "^4.5.4",
    "vite": "^2.7.4",
    "vue-tsc": "^0.29.8"
  }
}
